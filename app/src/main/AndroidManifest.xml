<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eo.app.eo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".calculator.CalculatorActivity" />
        <activity android:name=".member.activity.JoinActivity" />
        <activity android:name=".member.activity.LoginActivity" />
        <activity android:name=".member.activity.UpdateActivity" />
        <activity android:name=".member.activity.DeleteActivity" />
        <activity android:name=".member.activity.DetailActivity" />
        <activity android:name=".member.activity.ListActivity" />
        <activity android:name=".member.activity.CountActivity"></activity>
    </application>

</manifest>